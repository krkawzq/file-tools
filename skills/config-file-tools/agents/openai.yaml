interface:
  display_name: "Configure File Tools"
  short_description: "Install, build, and verify File Tools across agent hosts"
  default_prompt: "Use $config-file-tools to install or rebuild File Tools in the runtime root used by my selected agent host, then verify MCP discovery."
