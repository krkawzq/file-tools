interface:
  display_name: "Configure File Tools"
  short_description: "Install and verify the File Tools plugin runtime"
  default_prompt: "Use $config-file-tools to install the latest File Tools release into my agent host's plugin-local .venv and verify MCP discovery."
