interface:
  display_name: "File Tools"
  short_description: "Local/SSH file ops and bounded shell via MCP"
  default_prompt: "Use $file-tools with explicit cwd for local or SSH read/edit/patch/write and bounded bash."
